# Job-search configuration
# Keep real values in GitHub Actions Secrets or a local .env file.

JOB_SEARCH_ENABLED=true
JOB_SEARCH_MAX_RESULTS=10

# Example only — do not put your private target compensation in source control.
MIN_COMPENSATION_INR=

# Optional provider/API credentials
JOB_PROVIDER_API_KEY=
LLM_API_KEY=
